{
  "name": "@zed-mpe/browser-preview",
  "version": "0.1.0",
  "private": true,
  "description": "Browser preview shell for the Zed Markdown Preview Enhanced workflow.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --environment jsdom",
    "test:watch": "vitest --environment jsdom"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}